// Generated by CoffeeScript 1.12.7
(function() {
  var Reply;

  Reply = module.exports = function(session, id) {
    var handler;
    handler = function(text) {
      return handler.message(text);
    };
    handler.__proto__ = Reply;
    handler.session = session;
    handler.id = id;
    return handler;
  };

  Reply.message = function(args) {
    return this.session.sendReply(this.id, args);
  };

  Reply.error = function(text) {
    return this.session.sendError(this.id, text);
  };

  Reply.notify = function(method, args) {
    return this.session.sendNotification(method, args);
  };

  module.exports = Reply;

}).call(this);
